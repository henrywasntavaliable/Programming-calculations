def avg_of_nums():
    count = int(input("Please enter the amount of numbers"))
    total = 0 
    for i in range (count): 
        number = float(input(f"Enter number {i + 1}: "))
        total += number
        average = total / count 
        print (average)

avg_of_nums()
